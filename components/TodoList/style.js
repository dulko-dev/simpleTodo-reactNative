import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    content: {
      marginTop: 7,
      padding: 10,
      borderRadius: 8,
      borderWidth: 1,
      borderStyle: "dashed",
      borderColor: "#ff521b",
    },
  });

  export default styles;